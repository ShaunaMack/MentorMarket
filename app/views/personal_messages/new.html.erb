<h1>Start conversation with <%= @receiver.username %></h1>

<%= simple_form_for @personal_message do |f| %>
  <%= hidden_field_tag 'receiver_id', @receiver.id %>

  <%= f.label :body %>
  <%= f.text_area :body, required: true, placeholder: "Hi #{@receiver.username}..."%>
  <%= f.submit %>
<% end %>